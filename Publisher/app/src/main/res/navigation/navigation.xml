<?xml version="1.0" encoding="utf-8"?>

<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto" android:id="@+id/navigation"
    app:startDestination="@id/navigation_publisher">

    <fragment
        android:id="@+id/navigation_publisher"
        android:name="com.willy.publisher.publisher.PublisherFragment"
        android:layout="@layout/fragment_publisher" >
    </fragment>

    <dialog
        android:id="@+id/navigation_dialog"
        android:name="com.willy.publisher.dialog.PublishDialog"
        android:label="AddArticleDialog">

    </dialog>


</navigation>